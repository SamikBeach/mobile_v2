module.exports = {
  // 세미콜론 사용
  semi: true,

  // 싱글 쿼트 사용
  singleQuote: true,

  // 후행 쉼표 추가
  trailingComma: 'es5',

  // 라인 길이 설정
  printWidth: 100,

  // 탭 너비 설정
  tabWidth: 2,

  // 스페이스 사용 (탭 대신)
  useTabs: false,

  // 브라켓 간격
  bracketSpacing: true,

  // 브라켓을 같은 라인에
  bracketSameLine: false,

  // 화살표 함수 괄호
  arrowParens: 'avoid',

  // JSX에서 싱글 쿼트 사용
  jsxSingleQuote: true,
};
